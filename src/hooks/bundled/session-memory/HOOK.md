---
name: session-memory
description: "Saves session context to memory when /new or /reset is issued"
metadata: { "openclaw": { "emoji": "💾", "events": ["command:new", "command:reset"] } }
---
Saves a timestamped entry to the group's daily memory log when a session resets.
